<div class="flex flex-col justify-center items-center">
  <div class="text-5xl font-bold p-6"> Shop </div>
    <div class="flex flex-wrap">
    <% @products.each do |product| %>
      <div class="flex flex-col items-center justify-center border-red-400 border-dotted border-2  p-4 m-3 bg-white rounded-lg border border-amber-600">
        <a href="<%=product_path(product)%>">
            <%= image_tag product.product_image, class: "" %>
            <h2 class="flex justify-center font-bold text-xl"><%= product.product_name %></h2>
            <div class="flex flex-row justify-between">
              <div class="text-lg">Price: ₱ <%= product.price %></div>
              <span class="inline-flex justify-center items-center p-3 ml-3 w-3 h-3 text-sm font-medium text-blue-600 bg-blue-200 rounded-full dark:bg-blue-900 dark:text-blue-200"><%= product.stocks %></span>
            </div>
        </a>
        </div>
    <% end %>
    </div>
</div>